<!DOCTYPE html>
<html>
<head>
  <style type="text/css">
  html{overflow-y:hidden; background-color:#fff;width: 100%;height: 100%;margin: 0;padding: 0;}
  body{width: 100%;height: 100%;margin: 0;padding: 0; }
  .loginContainer_outer{
    max-width: 100%;
    width: 100%; 
    max-height: 100%; 
    height: 100%;
    background: #ffffff url('../login_bg.jpg') no-repeat fixed top center ;
    background-size: 100%;
    margin:0;
    padding:0;
    border:none;
  }
  .loginContainer{
    margin: auto;
    width: auto;
    text-align: center;
  }
</style>
</head>
<body>
  <div class="loginContainer_outer" data-original="../login_bg.jpg">
  <div class="loginContainer">

  <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: {role: "form"}) do |f| %>
    <div class="userContainer inputDiv">
        <%= f.text_field :mobile, class: "form-control", placeholder: "手机号", autofocus: true %>
      </div>
      <div class="passContainer inputDiv">
        <%= f.password_field :password, class: "form-control", placeholder: "密码", autocomplete: "off" %>
      </div>
      <%= content_tag(:div, flash[:alert], :id => "flash_alert") if flash[:alert] %>
       <div class="sub_btn">
          <%= f.submit "登录" %>
        </div>
        <%= link_to "注册", new_user_registration_path %>
  <% end %>
 </div>
 </div>
</body>
</html>
