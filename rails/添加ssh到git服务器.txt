*添加本地ssh到git服务器
1.登录git服务器
  ssh wujj@115.29.174.239 -p 1039
2.切换到root用户
  sudo su
3.进入git用户目录
  cd /home/git/.ssh
4.编辑文件
  vim authorized_keys  
5.将本机id_rsa.pub中的ssh复制替换进去  按esc，shift+:,输入wq保存，输入exit退出

*git clone git@115.29.174.239:insight4hr_talenkr_server.git

#使用rails shell命令查看数据库
登录服务器ssh wujj@115.29.46.56 -p 1039
1.RAILS_ENV=xxx rails c
2.cat config/database.yml
  mysql -uroot -p
#开启delayed_jobs
RAILS_ENV=xxx bundle exec rake jobs:work
#开启终端多窗口ctrl+shift+T
#终端命令输入历史ctrl+R
#关闭服务器passenger stop -p 3000
#开启服务器passenger start -a 127.0.0.1 -p -e house -d

